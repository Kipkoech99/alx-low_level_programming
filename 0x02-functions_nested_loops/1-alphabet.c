#include "main.h"

/**
 * main - check the code
 *
 * print_alphabet(void) - Prints the alphabet in lowercase.
 * Return: Always 0.
 */

int main(void)

{
	print_alphabet(void);
	{
		char c;

		for (c = 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
	}

	return (0);

}
