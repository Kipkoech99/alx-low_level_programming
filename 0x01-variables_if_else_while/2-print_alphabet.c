#include <stdio.h>

/**
 * main - Entry point for the program
 *
 * Result: Always 0 (success)
 */

int main(void)

{

	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);

{
